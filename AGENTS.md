# AGENTS

## OpenSpec Language Policy

- All OpenSpec artifacts (`proposal.md`, `design.md`, `specs/**/*.md`, `tasks.md`) MUST be written in Simplified Chinese by default.
- If the user explicitly requests another language for a specific change, follow the user request for that change only.
